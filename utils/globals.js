const BACKEND_URL = 'http://localhost:3030';

const LOCALSTORAGE_KEYS = {
    user: 'user',
    role: 'role',
    selectedCourse: 'selected course'
};
const ROLES = {
    lecturer: 'lecturer',
    student: 'student'
}

module.exports = {
    BACKEND_URL,
    LOCALSTORAGE_KEYS,
    ROLES
}
